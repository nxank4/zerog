{
  "name": "zerog",
  "displayName": "Zero-G",
  "description": "Experience weightless coding with local AI",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.103.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onView:zerog.chatView",
    "onView:zerog.historyView"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "zerog-sidebar",
          "title": "Zero-G AI",
          "icon": "$(rocket)"
        }
      ]
    },
    "views": {
      "zerog-sidebar": [
        {
          "type": "webview",
          "id": "zerog.chatView",
          "name": "AI Chat"
        },
        {
          "id": "zerog.historyView",
          "name": "History"
        }
      ]
    },
    "commands": [
      {
        "command": "zerog.openChat",
        "title": "Zero-G: Open AI Chat"
      },
      {
        "command": "zerog.inlineEdit",
        "title": "Zero-G: Inline Edit"
      },
      {
        "command": "zerog.acceptDiff",
        "title": "Accept AI Suggestion",
        "icon": "$(check)"
      },
      {
        "command": "zerog.discardDiff",
        "title": "Discard AI Suggestion",
        "icon": "$(close)"
      },
      {
        "command": "zerog.newChat",
        "title": "Zero-G: New Chat",
        "icon": "$(add)"
      },
      {
        "command": "zerog.openSession",
        "title": "Zero-G: Open Session"
      },
      {
        "command": "zerog.deleteSession",
        "title": "Delete Session",
        "icon": "$(trash)"
      },
      {
        "command": "zerog.renameSession",
        "title": "Rename Session",
        "icon": "$(edit)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "zerog.newChat",
          "when": "view == zerog.historyView",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "zerog.renameSession",
          "when": "viewItem == session",
          "group": "inline@1"
        },
        {
          "command": "zerog.deleteSession",
          "when": "viewItem == session",
          "group": "inline@2"
        },
        {
          "command": "zerog.renameSession",
          "when": "viewItem == session",
          "group": "1_modification@1"
        },
        {
          "command": "zerog.deleteSession",
          "when": "viewItem == session",
          "group": "1_modification@2"
        }
      ],
      "editor/title": [
        {
          "command": "zerog.acceptDiff",
          "group": "navigation@1",
          "when": "zerog.diffReviewActive"
        },
        {
          "command": "zerog.discardDiff",
          "group": "navigation@2",
          "when": "zerog.diffReviewActive"
        }
      ]
    },
    "keybindings": [
      {
        "command": "zerog.inlineEdit",
        "key": "ctrl+alt+k",
        "mac": "cmd+k",
        "when": "editorTextFocus && editorHasSelection"
      }
    ],
    "configuration": {
      "title": "Zero-G",
      "properties": {
        "zerog.baseUrl": {
          "type": "string",
          "default": "http://localhost:8080",
          "description": "Base URL for the Antigravity AI proxy (matches ANTHROPIC_BASE_URL)"
        },
        "zerog.authToken": {
          "type": "string",
          "default": "test",
          "description": "Authentication token for the AI proxy (matches ANTHROPIC_AUTH_TOKEN)"
        },
        "zerog.model": {
          "type": "string",
          "default": "claude-opus-4-6-thinking",
          "description": "AI model to use (matches ANTHROPIC_MODEL)"
        },
        "zerog.systemPrompt": {
          "type": "string",
          "default": "You are a helpful coding assistant.",
          "description": "System prompt for the AI assistant"
        },
        "zerog.enableAutocomplete": {
          "type": "boolean",
          "default": true,
          "description": "Enable AI-powered ghost text autocomplete (Copilot-style inline completions)"
        },
        "zerog.maxTokens": {
          "type": "number",
          "default": 4096,
          "description": "Maximum number of tokens in chat responses"
        },
        "zerog.autocompleteDelay": {
          "type": "number",
          "default": 300,
          "description": "Delay in milliseconds before triggering autocomplete"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.103.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.27.2",
    "eslint": "^9.39.2",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.54.0"
  },
  "dependencies": {
    "@types/markdown-it": "^14.1.2",
    "axios": "^1.13.5",
    "highlight.js": "^11.11.1",
    "markdown-it": "^14.1.0"
  }
}
